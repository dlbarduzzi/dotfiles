# vim

### Before getting started

Consider running the following commands for a fresh vim configuration.

```sh
mv ~/.vimrc ~/.vimrc.bak_$(date +%Y%m%d%H%M%S)
```

### Create vim symlink

```sh
ln -s $(pwd)/vim/.vimrc ~/.vimrc
```
